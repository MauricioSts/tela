appId: com.mauriciosts.mimo
productName: MIMO
directories:
  buildResources: build
  output: dist
files:
  - main.js
  - preload-site.js
  - preload-picker.js
  - picker.html
  - package.json
  - lib/**/*
  # o addon de áudio por processo mora aqui; o electron-builder tira os .node do
  # asar sozinho e o @electron/rebuild recompila pra ABI do Electron
  - node_modules/**/*
win:
  target:
    - target: nsis
      arch: [x64]
  icon: build/icon.ico
  artifactName: MIMO-${version}-Setup.${ext}
nsis:
  # instala em %LOCALAPPDATA% pro usuário atual: sem UAC, sem pedir senha de admin
  oneClick: true
  perMachine: false
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: MIMO
  deleteAppDataOnUninstall: false
publish:
  provider: github
  owner: MauricioSts
  repo: tela
  releaseType: release
