# TLS automatico via Let's Encrypt.
# O app MIMO (server.js) serve TUDO: HTML dinamico (injeta token por request),
# API REST /api/* e a sinalizacao WebSocket /ws. Por isso e reverse_proxy do host inteiro.

tela.mauriciosts.com {
	encode zstd gzip
	reverse_proxy localhost:8080
}
